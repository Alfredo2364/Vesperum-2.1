name: vesperum
description: "Juego gótico de acción con estética pixel art inspirado en Castlevania y Shadow Fight 2."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Librerías principales
  flame: ^1.33.0           # Motor de juego 2D
  flame_audio: ^2.11.11    # Sonido para Flame
  just_audio: ^0.10.5      # Música en loop (para pantallas normales)
  supabase_flutter: ^2.10.3  # Conexión con Supabase (DB y auth)
  flutter_animate: ^4.5.2  # Animaciones suaves en UI
  google_fonts: ^6.3.2     # Tipografías tipo pixel art
  cupertino_icons: ^1.0.8  # Iconos iOS (uso general)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    # 🎨 Fondos del lobby (Capilla Lunar)
    - assets/images/lobby/bg_layer_0.png
    - assets/images/lobby/bg_layer_1.png
    - assets/images/lobby/bg_layer_2.png
    - assets/images/lobby/fg_candles.png

    # 🧍 UI y personajes
    - assets/images/ui/avatar_default.png

    # 🔊 Música y efectos
    - assets/audio/requiem_eclipse.ogg

  fonts:
    # Fuente tipo pixel art (usa Press Start 2P desde google_fonts)
    - family: PressStart2P
      fonts:
        - asset: fonts/PressStart2P-Regular.ttf
